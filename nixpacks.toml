[phases.install]
cmds = [
  "cd client && npm ci",
  "cd server && npm ci"
]

[phases.build]
cmds = [
  "cd client && npm run build",
  "cd server && npx prisma generate --schema=src/prisma/schema.prisma",
  "cd server && npm run build"
]
